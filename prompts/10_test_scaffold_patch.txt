Erzeuge einen **unified git patch** (nur Patch ausgeben, kein Fliesstext), der folgendes in diesem Repo anlegt/ergaenzt:

1) Basis fuers Testen
- Root: package.json Scripts: 
  "test": "pnpm -r test",
  "test:unit": "vitest run --coverage",
  "test:int": "vitest run --coverage --dir tests/integration",
  "test:watch": "vitest",
  "coverage:report": "vitest run --coverage"
- Root: vitest.config.ts mit tsconfig-paths Unterstuetzung, JSDOM fuer Libs, Node-Umgebung fuer Server, Coverage Gates: 90/90/90/90.
- Root: tsconfig.base.json ggf. so anpassen, dass Tests (tests/**) und *.d.ts sauber kompiliert werden.

2) Ordnungsstruktur Tests
- tests/unit/.keep
- tests/integration/.keep
- tests/helpers/test-ports.ts (Hilfsportfinder, z. B. mit get-port)

3) DevDeps & Konfig
- Fuege (falls nicht vorhanden) devDependencies hinzu: vitest, @types/node, tsx, get-port, tsconfig-paths.
- Kein Docker, kein Ansible, keine CI-Dateien.

Anforderungen:
- ESM only, TypeScript strikt.
- Keine bestehenden Laufzeitpfade brechen.
- Patch MUSS mit "diff --git" beginnen und vollstaendig anwendbar sein.

