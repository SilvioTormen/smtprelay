# CRITICAL SECURITY CONFIGURATION
# CHANGE ALL VALUES BEFORE PRODUCTION DEPLOYMENT!

# Node Environment
NODE_ENV=production

# JWT Secrets (MUST be changed!)
JWT_SECRET=CHANGE_THIS_USE_openssl_rand_-base64_64
JWT_REFRESH_SECRET=CHANGE_THIS_USE_openssl_rand_-base64_64

# Session Secret (MUST be changed!)
SESSION_SECRET=CHANGE_THIS_USE_openssl_rand_-base64_64

# Database Passwords (MUST be changed!)
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
REDIS_PASSWORD=CHANGE_THIS_STRONG_PASSWORD

# Exchange Online OAuth2 (from Azure AD)
AZURE_TENANT_ID=your-actual-tenant-id
AZURE_CLIENT_ID=your-actual-client-id
AZURE_CLIENT_SECRET=your-actual-client-secret

# FIDO2/WebAuthn Configuration
RP_ID=your-domain.com
ORIGIN=https://your-domain.com

# Admin Account (first run only)
ADMIN_INITIAL_PASSWORD=CHANGE_THIS_COMPLEX_PASSWORD_123!
HELPDESK_INITIAL_PASSWORD=CHANGE_THIS_COMPLEX_PASSWORD_456!

# Security Settings
ENFORCE_MFA=true
PASSWORD_MIN_LENGTH=12
SESSION_TIMEOUT=1800000
MAX_LOGIN_ATTEMPTS=3
LOCKOUT_DURATION=1800000

# HTTPS/TLS
TLS_CERT_PATH=/path/to/cert.pem
TLS_KEY_PATH=/path/to/key.pem
FORCE_HTTPS=true

# Monitoring
SENTRY_DSN=your-sentry-dsn-if-using